Index: resources/views/livewire/report-generator.blade.php
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/resources/views/livewire/report-generator.blade.php b/resources/views/livewire/report-generator.blade.php
--- a/resources/views/livewire/report-generator.blade.php	
+++ b/resources/views/livewire/report-generator.blade.php	(date 1742708744939)
@@ -30,6 +30,25 @@
                 </div>
             @endif
 
+                <form method="GET" action="">
+                    <label for="year">Year:</label>
+                    <select name="year" id="year">
+                        @for ($y = now()->year - 5; $y <= now()->year; $y++)
+                            <option value="{{ $y }}" {{ request('year', now()->year) == $y ? 'selected' : '' }}>{{ $y }}</option>
+                        @endfor
+                    </select>
+
+                    <label for="quarter">Quarter:</label>
+                    <select name="quarter" id="quarter">
+                        @for ($q = 1; $q <= 4; $q++)
+                            <option value="{{ $q }}" {{ request('quarter', ceil(now()->month / 3)) == $q ? 'selected' : '' }}>Q{{ $q }}</option>
+                        @endfor
+                    </select>
+
+                    <button type="submit">Apply</button>
+                </form>
+
+
             @if(isset($cropDistributionChart))
                 <div class="bg-white p-4 shadow rounded-lg">
                     <h3 class="text-lg font-bold text-gray-700 mb-2">Crops</h3>

Index: resources/views/livewire/report-generator.blade.php
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/resources/views/livewire/report-generator.blade.php b/resources/views/livewire/report-generator.blade.php
--- a/resources/views/livewire/report-generator.blade.php	
+++ b/resources/views/livewire/report-generator.blade.php	(date 1741443545452)
@@ -4,7 +4,7 @@
     <div class="p-6 bg-white shadow-md rounded-lg">
         <h2 class="text-2xl font-bold mb-6 text-gray-800">Harvest Report</h2>
 
-        <!-- Date Range Filters -->
+        <!-- Date Range Filters (does not work)-->
         <div class="flex flex-wrap items-center gap-4 mb-6">
             <div>
                 <label for="startDate" class="block text-sm font-medium text-gray-700">Start Date</label>
@@ -19,7 +19,6 @@
             <button wire:click="updateReport" class="px-4 py-2 bg-green-600 text-white rounded-md shadow hover:bg-green-700 transition">
                 Filter
             </button>
-
         </div>
 
         <!-- Total Yield Summary -->
@@ -44,7 +43,6 @@
             @if(isset($timeComparisonChart))
                     <div class="bg-white p-4 shadow rounded-lg">
                         <h3 class="text-lg font-bold text-gray-700 mb-2">Time Comparison</h3>
-                        <!-- Aspect Ratio Container for Responsive Chart -->
                         <div wire:ignore class="relative aspect-video">
                             <canvas id="timeComparisonChartCanvas" class="absolute inset-0"></canvas>
                         </div>
@@ -54,7 +52,6 @@
             @if(isset($fieldProductivityChart))
                     <div class="bg-white p-4 shadow rounded-lg">
                         <h3 class="text-lg font-bold text-gray-700 mb-2">Field Results</h3>
-                        <!-- Aspect Ratio Container for Responsive Chart -->
                         <div wire:ignore class="relative aspect-video">
                             <canvas id="fieldProductivityChartCanvas" class="absolute inset-0"></canvas>
                         </div>
@@ -64,7 +61,6 @@
             @if(isset($cropDistributionChart))
                     <div class="bg-white p-4 shadow rounded-lg">
                         <h3 class="text-lg font-bold text-gray-700 mb-2">Crops</h3>
-                        <!-- Aspect Ratio Container for Responsive Chart -->
                         <div wire:ignore class="relative aspect-video">
                             <canvas id="cropDistributionChartCanvas" class="absolute inset-0"></canvas>
                         </div>
@@ -99,8 +95,7 @@
                     </tbody>
                 </table>
             </div>
-
-            <!-- Pagination -->
+            
             <div class="mt-4">
                 {{ $harvestRecords->links('pagination::tailwind') }}
             </div>
@@ -111,7 +106,6 @@
         <script>
             document.addEventListener("DOMContentLoaded", function () {
                 let charts = {};
-
                 function createChart(canvasId, type, data, options) {
                     let canvas = document.getElementById(canvasId);
                     if (!canvas) return;
@@ -123,7 +117,6 @@
 
                     charts[canvasId] = new Chart(ctx, { type: type, data: data, options: options });
                 }
-
                 function initCharts() {
                     @if(isset($totalYieldChart) && isset($totalYieldChart['labels']) && count($totalYieldChart['labels']) > 0)
                     createChart("totalYieldChartCanvas", "line", {
@@ -184,14 +177,11 @@
                     });
                     @endif
                 }
-
                 initCharts();
-
                 Livewire.hook('message.processed', () => {
                     initCharts();
                 });
             });
         </script>
     @endpush
-
 @endsection
